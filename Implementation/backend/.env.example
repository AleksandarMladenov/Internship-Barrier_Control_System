# ----- App -----
# dev | staging | prod
ENV=dev
APP_PORT=8000

# ----- Database (two options) -----
# OPTION A) Provide a single DATABASE_URL (recommended for CI/prod)
# DATABASE_URL=postgresql+psycopg://<USER>:<PASSWORD>@<HOST>:<PORT>/<DBNAME>

# OPTION B) Or provide connection parts (the app will build DATABASE_URL)
POSTGRES_USER=parking
POSTGRES_PASSWORD=parking
POSTGRES_DB=parking
# When running with Docker Compose, the DB host is the service name "db"
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ----- Payments (leave empty until you wire Stripe) -----
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=

# ----- Google/Firebase (set only when you need them) -----
# Path to your mounted service-account file (use Docker secrets or your OS keychain)
GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/firebase_sa
FIREBASE_STORAGE_BUCKET=
